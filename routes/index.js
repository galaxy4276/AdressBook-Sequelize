const HOME = '/';

const EDIT_ADRESS = '/edit';

const LOGIN = '/login';
const JOIN = '/join';
const LOGOUT = '/logout'

const urlBox = {
  home: HOME,
  editAdress: EDIT_ADRESS,
  login: LOGIN,
  join: JOIN,
  logout: LOGOUT,
};

export default urlBox;